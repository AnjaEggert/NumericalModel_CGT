start_date         = datenum(2019,06,01); % initial date
end_date           = datenum(2019,08,01); % final date
repeated_runs      = 1;                   % how often the same forcing period is repeated
timestep           = 3.0/24;              % timestep [days], here every 3 hours
output_interval    = 3.0/24;              % output interval [days]